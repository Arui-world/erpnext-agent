# Security tests

The next implementation phase adds two-user OAuth/MCP isolation, cross-user Action rejection,
prompt-injection fixtures, refresh single-flight and write-timeout recovery tests here.

