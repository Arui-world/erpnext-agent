# Integration tests

This directory is reserved for tests that run against Redis, PostgreSQL and an authenticated
ERPNext MCP endpoint. Unit tests deliberately do not use a shared ERPNext service credential.

